import React, { useState } from 'react';
import Chat from './Chat';

const ChatApp = () => {
  //   const [recipientId, setRecipientId] = useState(null);
  //   // Danh sách giả định người dùng
  //   const users = [
  //     { id: 1, name: 'Alice' },
  //     { id: 2, name: 'Bob' },
  //     { id: 3, name: 'Charlie' },
  //   ];
  //   return (
  //     <div>
  //       <h1>Select a user to chat with:</h1>
  //       <ul>
  //         {users.map((user) => (
  //           <li key={user.id}>
  //             <button onClick={() => setRecipientId(user.id)}>Chat with {user.name}</button>
  //           </li>
  //         ))}
  //       </ul>
  //       {recipientId && <Chat userId={1} recipientId={recipientId} />}
  //     </div>
  //   );
};

export default ChatApp;
